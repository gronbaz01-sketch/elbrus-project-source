# SPA fallback: неизвестные пути (кроме уже существующих статических файлов/папок,
# включая prerender-папки /prielbrusye/, /aktivnosti/) отдаём на index.html,
# чтобы client-side роутинг (react-router) сам показал нужную страницу или 404.
#
# Поддерживается Netlify и Cloudflare Pages "из коробки". Если хостинг Lovable
# использует другой механизм — правило нужно перенести в его формат (см. документацию хостинга).
/*  /index.html  200
